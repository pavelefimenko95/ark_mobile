import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        flex: 1
    },
    centralize: {
        justifyContent: 'center',
        alignItems: 'center'
    }
});